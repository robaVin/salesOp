// Deprecated: the Zone renderer was generalised into the ContainerRenderer,
// which serves system zones, user workspaces, and future container kinds.
// Kept as a re-export so any lingering imports keep resolving.
export { Container as Zone } from '../Container'
