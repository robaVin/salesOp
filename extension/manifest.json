{
  "manifest_version": 3,
  "name": "Sales Canvas — Capture",
  "version": "0.2.0",
  "description": "Press Alt+Shift+S anywhere to capture selected text (or the open Gmail thread) as a note on Sales Canvas. Configure your API token in the popup.",
  "permissions": ["activeTab", "scripting", "notifications", "tabs", "storage"],
  "host_permissions": [
    "http://localhost:3001/*",
    "http://localhost:5173/*",
    "https://mail.google.com/*"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_title": "Sales Canvas Capture",
    "default_popup": "popup.html"
  },
  "commands": {
    "capture_to_canvas": {
      "suggested_key": {
        "default": "Alt+Shift+S"
      },
      "description": "Capture selection or Gmail thread to Sales Canvas"
    }
  }
}
